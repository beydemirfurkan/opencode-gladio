import { describe, it, expect } from "bun:test";

describe("installer internal helpers", () => {
  it("placeholder: mergePluginList is internal and tested via integration", () => {
    expect(true).toBe(true);
  });
});
